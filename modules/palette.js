import {
    hexToHSL,
    copyhexCode,
    displayHexCode,
    rgbToHex,
    filterRGBColor,
    splitComa,
    createGrid,
} from "./utils.js";
import kwulers from 'kwulers';

export default class Palette {
    constructor(name) {
        this.name = name.replace(/[^a-zA-Z0-9]/g,'-');
    }
    cretaePalette() {

    }

    generatePalette() {

    }

    removePalette() {

    }

}
